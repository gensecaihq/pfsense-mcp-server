# Simplified pfSense MCP Server Configuration
# Copy to .env and update with your values

# === pfSense Connection ===
PFSENSE_URL=https://your-pfsense.example.com
PFSENSE_VERSION=ce               # "ce" or "plus"
PFSENSE_CONNECTION_METHOD=rest   # "rest", "xmlrpc", or "ssh"

# === REST API Authentication ===
# For CE: API Key + Secret
# For Plus: API Key only (used as Bearer token)
PFSENSE_API_KEY=your-api-key-here
PFSENSE_API_SECRET=your-api-secret-here  # CE only

# === XML-RPC Authentication ===
# Legacy method - username/password
PFSENSE_USERNAME=admin
PFSENSE_PASSWORD=your-password-here

# === SSH Authentication ===
# Advanced method - direct command execution
PFSENSE_SSH_HOST=your-pfsense.example.com
PFSENSE_SSH_PORT=22
PFSENSE_SSH_USERNAME=admin
SSH_KEY_FILE=./config/ssh/id_rsa        # Path to SSH private key
# Or use password:
# PFSENSE_SSH_PASSWORD=your-ssh-password

# === Optional Settings ===
VERIFY_SSL=true                  # Set to false for self-signed certificates
CONNECTION_TIMEOUT=30            # Connection timeout in seconds
CACHE_TTL=300                   # Cache TTL in seconds
MCP_PORT=8000                   # Port for HTTP mode